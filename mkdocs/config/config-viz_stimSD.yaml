'''script du tool qui va permettre d'unifier et d'automatiser les visualisations que l'on doit faire apres avoir fait des simulations simnibs"


# 
subjects:


paths:


# 1ere partie pour definir les images disponible à superposer pour la creation des vols 
# (tout ca est geré facileemnt grace au reader + les transform mni-suject sil le faut)

# for later ! space: mni or native 
available-vols: # for later (differencier les masks des vols a valeurs continue / valeur discrete mais non binaire ? signé ou non)
  anat: (T1 ou label_prep) #brain mask ou tout le skull...
  ROI: (x,y,z coord en mni) | atlas + list | file (a voir comment gerer ça proprement... commencons simple)
  fields: e ou E ou j ou J
    scale: min, max
  #later lesion: 





# un block = un type de figures que l'on veut faire 
nifti_figures:

  viz-name: "Anat-with-ROI"
  vols: [anat,ROI] # list des images aue l'on veut superposé ? #pour linstant cacher le dict niivue de l'UI ?
  type: 2D (or3D)
  subtype: slicing parallele or ortho
  cut_coords: #mni space, le script soccupe denvoyer en native si besoin
  axis (axial et ou coronal et ou tangentiel):
  nombre de coupe:    #if parallel... agerer dans la verif de format, pas dans l'UI
  distance entre coupe depuis le centre:    #if parallel... à gerer dans la verif de format, pas dans l'UI
  #for later : display electrodes...
  #cohorts figures: yes
    #some parameters ? for later...

  viz-name: ""
  vols:
  type: 2D or 3D
  default_3Dcamera: (les deux angles tel que demandé par render 3D)
  if_exists: str = "overwrite",


  #... mettre ce qui est actuellement dans run-viz.py


#stats_figures (later)


